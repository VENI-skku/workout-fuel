# 인수인계 (HANDOFF) — 여기부터 읽으세요

프로젝트: 운동별 소모 칼로리 → 간편식 추천 서비스
문서 3개: **HANDOFF.md(이 문서, 시작점)** · README.md(기술 상세) · index.html(서비스 본체)

---

## 1. 지금까지 된 것  (유담 / 배포·운영환경 담당)

- 서비스 = `index.html` (용준 개발 1안). 정적 웹이라 서버·DB 없음
- 배포: Netlify (드래그 또는 GitHub 연동)
- 데이터 수집: **GA4(구글 애널리틱스)** 심음 — 방문 + 핵심 전환 `recommend_click` 자동 기록
- (선택) Microsoft Clarity — 세션 녹화·히트맵으로 "어디서 막히는지" 관찰

---

## 2. 접속 정보  ⚠️ 여기부터 채워야 다음 사람이 이어받음

| 항목 | 값 | 로그인 주인 |
|---|---|---|
| 배포 URL | https://skku137ai.netlify.app/ | 유담(VENI-skku) |
| GitHub repo | https://github.com/VENI-skku/workout-fuel | 유담(VENI-skku), 승주(SeungJu-Lee0422, Admin 초대됨), 유나(yunaa-03, Admin 초대됨) |
| Netlify 사이트 | skku137ai | 유담(VENI-skku) — 승주·유나 팀 멤버 초대 예정 |
| GA4 측정 ID | G-89F38STC0C | 유담 |
| GA4 속성 접근권 | 승주(l31433077@gmail.com), 유나(yuna8670@gmail.com) 추가 예정(브라우저에서 직접 진행) | 유담 |
| Clarity(선택) | ____________ |  |

> GA4·Netlify 로그인 주인을 꼭 적어둘 것. 다음 사람이 접근 못 하면 여기서 막힘.

---

## 3. 다음 사람별 할 일

### → 승주 (데이터 분석 · 영상 4번 시각화)
- 데이터는 **GA4에 쌓임.** 볼 이벤트:
  - `page_view` — 방문 (자동)
  - `recommend_click` — **핵심 전환.** 파라미터: `exercise`, `kcal`, `tier`, `product`, `mvp_version`
- KPI 정의·계산식 → README의 "KPI 정의" 표 참고
- 뽑는 법: GA4 → 보고서 > 참여도 > 이벤트 (또는 탐색 > 자유 형식) → CSV 내보내기 → 엑셀
- 영상 4번 추천 차트: ① 전환율(클릭÷방문) ② 운동종목별 클릭 ③ 티어별 분포 ④ 날짜별 방문 추이 ⑤ **mvp_version별 1.0 vs 2.0 비교**
- 접근 안 되면: GA4 관리 > 속성 액세스 관리에서 계정 추가받기

### → 유나 (MVP 수정 후 재배포)
재배포 순서 (5단계):
1. `index.html` 수정
2. `index.html` 위쪽의 **`APP_VERSION`을 `"mvp-2.0"`으로 변경** → 데이터가 1.0/2.0으로 자동 구분됨
3. README "MVP 2.0" 칸 채우기 (뭘·왜 바꿨는지, 어떤 피드백 때문인지)
4. 배포: `git push`(자동 재배포) 또는 Netlify에 폴더 다시 드래그
5. GA4 실시간 보고서에서 반영 확인
- ⚠️ **GA4 측정 ID는 절대 바꾸지 말 것** → 그래야 1.0 vs 2.0 지표 비교가 됨

### → 건학 (PPT 정리)
순서대로 캡처할 화면:
1. 서비스 화면 (용준)
2. 배포·GA4 세팅 화면 (유담)
3. 실사용 데이터 / GA4 대시보드
4. 1.0 → 2.0 변경점 (+ GitHub 커밋 히스토리 = 발전 과정 증거)
5. 지표 비교 (승주 결과물)
- 스토리 축: "완벽함"이 아니라 **"피드백으로 고쳐나간 과정"** (기획서 핵심 포인트)

---

## 4. 발표(영상 3번, 유담 본인 파트) 체크리스트
- 배포 파이프라인 → 데이터 수집 세팅 → 1.0→2.0 재배포 → **배포 중 겪은 문제·해결**
- 기획서 [중요]: 지표가 나빠졌어도 숨기지 말고 "뭘 배웠고 어떻게 반영했는지"가 고득점
